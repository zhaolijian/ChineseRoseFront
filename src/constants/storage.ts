/**
 * 存储相关常量
 */

/**
 * Storage key 前缀
 */
export const STORAGE_PREFIX = 'chinese_rose_'

/**
 * Token 存储 key
 */
export const TOKEN_KEY = `${STORAGE_PREFIX}token`

/**
 * 用户信息存储 key
 */
export const USER_INFO_KEY = `${STORAGE_PREFIX}userInfo`

/**
 * Token 过期时间（天）
 */
export const TOKEN_EXPIRE_DAYS = 7

/**
 * 倒计时存储 key
 */
export const COUNTDOWN_KEY = `${STORAGE_PREFIX}countdown`

/**
 * 草稿存储 key 前缀
 */
export const DRAFT_KEY_PREFIX = `${STORAGE_PREFIX}draft_`